process ProcessOne = {

  source = PoolSource{
    untracked vstring fileNames = {'/archive/ecal/h4tb.pool-cmssw-SM06/h4b.00016481.A.0.0.root'}
    #untracked int32 maxEvents = 10
    untracked bool isBinary = true
  }

 # unpack raw data from test beam
 # module ecalEBunpacker = EcalDCCUnpackingModule{ }

 # module for the CNA test
   module myCnaPackage = EcalCorrelatedNoisePedestalRunAnalyzer{

   untracked uint32 verbosity = 0

  #Getting Event Header
  # string eventHeaderProducer = "ecalEBunpacker"
   string eventHeaderProducer = "ecalTBunpack"
   string eventHeaderCollection = ""
   }

 #  path p = { ecalEBunpacker, myCnaPackage }
  path p = { myCnaPackage }

# endpath ep = { out }
}
